---
title: "Release 2026.1"
description: "Aiming for Smoother Upgrades"
weight: 20
---

This release note set is organized by component to make upgrade planning and validation easier.

## Platform Foundations

### Cert-Manager

#### New Features

- Allow other kubernetes deployment models including Talos k8s stack to include a managed cert-manager helm chart. Decouple the cert-manager installation from the base kubespray ansible roles and allow the official upstream charts to provide chart and CRD updates including patched image rollouts. This includes envoy gateway-api support and custom DNS server forwarders.

### Proxy Environment Handling

#### Bug Fixes

- A new override `force_proxy_setting` is introduced to control when proxy environment variables are added into the shell and systemd environment. In some circumstances, adding system-wide HTTP proxy environment variables can cause issues with dbus, rendering systemd inoperable and stopping all systemd operations including reboot. This can happen if the proxy server does not allow reverse connections or blocks requests to localhost when `NO_PROXY` is improperly configured.

### MariaDB Operator

#### Prelude

MariaDB Operator Helm charts have been upgraded from 0.38.1 to 26.3.0 following the recommended progressive upgrade path via 25.8.4 and 25.10.4.

Upgrade path:
    0.38.1 → 25.8.4 → 25.10.4 → 26.3.0

The version 25.8.4 and 25.10.4 is the latest patch release in the 25.8.x and 25.10.x series and includes all prior fixes along with additional stability improvements.

This upgrade introduces improvements to replication failover behaviour, backup and restore capabilities, operator stability, and compatibility with newer Kubernetes versions.

The 25.x release line establishes PhysicalBackup and improved replication handling, while 26.3.0 extends this with Point-in-Time Recovery (PITR), enhanced backup integrations, and Helm chart structural changes.

These improvements apply to both Galera and replication clusters.

#### New Features

- Physical backups introduced in the 25.8 release series via the PhysicalBackup CR.

  Supported mechanisms:
  * MariaDB-native backups using mariadb-backup
  * Kubernetes VolumeSnapshots for storage-level backup and restore

  Benefits:
  * Faster backup and restore operations
  * Reduced recovery time objectives (RTO)
  * Support for cluster bootstrapping via spec.bootstrapFrom

- Point-in-Time Recovery (PITR) support available in 26.3.0 using object storage backup logs and recovery targets.

- Bootstrap improvements and restore orchestration enhancements for Galera and replication deployments.

- Helm chart alignment for newer Kubernetes APIs and more robust chart packaging.

#### Upgrade Notes

- CRDs must be upgraded progressively and must never be deleted during operator upgrades.
- Sequential upgrade path is required due to schema evolution and behavioural changes between the chart series.
- Existing MariaDB workloads should be validated after each intermediate operator upgrade.

### PostgreSQL Operator

#### New Features

- Add support for monitoring-user creation for telemetry workflows.

#### Bug Fixes

- Fix issues related to postgres monitoring secret generation and user creation.

## Observability and Telemetry

### OpenTelemetry

#### New Features

- Add OpenTelemetry deployment and daemon collectors to support infrastructure and platform telemetry collection.
- Enable MySQL, RabbitMQ, and Memcached receivers by default.
- Add optional PostgreSQL and HTTPCheck receiver support via overrides.

#### Upgrade Notes

- Monitoring secrets are created and propagated during install to support receiver authentication.
- PostgreSQL telemetry requires the postgres monitoring user to exist and may require reinstalling the Postgres operator after enabling support.

### Tempo

#### New Features

- Add Tempo as the tracing backend for Genestack observability.
- Support default PVC-backed local storage and optional object storage overrides.

### Monitoring Documentation

#### New Features

- Expand monitoring and observability documentation to cover:
  * Grafana
  * Loki
  * Prometheus
  * Tempo
  * OpenTelemetry
  * OTel base metrics collection

## Kubernetes and Container Platform

### Labels and Scheduling

#### New Features

- Add `longhorn.io/storage-node=enabled` to the recommended node label guidance for storage-specific placement.

### Nested Virtualization

#### New Features

- Enable nested virtualization module initialization by default in libvirt on supported compute hosts.

## Networking and Load Balancing

### OVN

#### New Features

- Add OVN VLAN subinterface support and related metrics documentation improvements.

## Compute and Scheduling

### Blazar

#### New Features

- Add reservation splitter workflow to break out lease reservation events for downstream Ceilometer and Gnocchi processing.
- Add filter guidance for Blazar scheduling behavior.

### Masakari

#### New Features

- Add host-monitor documentation.
- Add instance introspection monitor documentation.

## Identity and Secrets

### Keystone LDAP

#### New Features

- Add dedicated Keystone LDAP documentation and Hugo-side destination content.

## Storage, Images, and Data Protection

### Freezer

#### New Features

- Expand Freezer operational guidance with backup-retention policy content.

#### Bug Fixes

- Fix merge-marker issues and improve client install/uninstall and retention workflow documentation.

### Ceph-backed Services

#### New Features

- Add dedicated operational documentation for:
  * Cinder Ceph store
  * Compute external Ceph store
  * Glance Ceph store

### Trove

#### New Features

- Add Trove service documentation and MySQL image guidance.

## Orchestration

### Designate

#### New Features

- Add Designate deployment documentation.
- Add Designate preparation guidance.
- Add Designate/Neutron integration guidance.
- Add Designate exporter documentation.
